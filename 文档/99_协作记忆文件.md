# AFK-RPG 协作记忆文件

## 1. 文档用途

本文件用于在对话上下文接近上限、需要切换工作上下文或后续接手时，快速恢复关键协作状态。

维护原则：

- 只记录高价值上下文，不做流水账
- 优先记录“已经确认的事实”“当前约定”“未完成事项”“重要路径”
- 当上下文接近上限时，优先更新本文件，再进入下一轮协作

## 2. 当前项目与环境

- 项目名：`AFK-RPG`
- 本地路径：`/Users/hehe/Documents/AIProject/AFK-RPG`
- 引擎版本：`Godot 4.6.1`
- 当前主场景：`res://scenes/main/game_root.tscn`
- 在线试玩地址：`https://jiuxiaoyijian.github.io/AFK-RPG/`
- GitHub 仓库：`https://github.com/jiuxiaoyijian/AFK-RPG`

## 3. 已确认的项目状态

- 项目可在本地 Godot 中打开并运行
- 当前运行日志没有 `missing resource`、`missing script` 或 `parse error`
- 已知主要运行警告为 `EventBus` 中若干 signal 的未显式使用 warning，不阻塞运行
- 工程已具备 Web 导出和 GitHub Pages 自动部署能力
- 本地工程内容已重新与 GitHub 最新仓库对齐
- `文档/Figma_交互设计简介.md` 与 `文档/Figma_分阶段提示词.md` 已补齐到本地项目
- `ComfyUI+LoRA/` 已从工程目录中移除
- `afk_rpg_formal` 正式资源的本地导入缓存已重新生成，`.ctex` 缺失 warning 已消失
- `文档/prototype/` 下已形成两份网页稿：
  - `index.html`：交互原型
  - `review.html`：静态评审展示页
- 已完成 RPG 化交互重构第一切片：
  - `InventoryPanel` 已从文本列表重构为 `纸娃娃 + 10x4 格子背包 + 详情/对比 + 快捷操作`
  - `CubePanel` 已从 `ItemList` 式结构重构为 `左分支 / 中详情 / 右执行` 的工坊三栏
  - HUD 已开始收口为 `当前 Build / 当前目标 / 已穿装备` 的 RPG 主循环表达
  - HUD 四张核心卡片已开始承担真正的入口职责，可把玩家直接送进 `背包 / 百炼坊 / 成长中心 / 推演与秘境`
- 游戏内相关 UI 已开始按网页稿样式回灌，当前重点已落到 HUD、底部导航栏、模态面板和结果卡
- 游戏内 UI 回灌已继续进入第二轮布局细化，当前 warning 已收回到只剩 `EventBus` signal warning
- 所有二级界面已完成首轮结构重排，当前包括 `背包 / 悟道 / 异闻录 / 推演 / GM / 闭关所得/破局要闻`
- 已完成暗黑like 重构的 `Wave 0 / Wave 1` 基线：
  - Figma 文档与网页原型已切到 `背包 / 百炼坊 / 江湖见闻录 / 成长中心 / 推演与秘境`
  - `BuildQueryService` 与 `LootRuleService` 已落地，`GameManager` / `LootCodexSystem` 的高耦合查询逻辑已开始外提
  - `ConfigDB` 已能读取 `sets / cube_recipes / gems / rift` 新配置
  - `SetSystem / CubeSystem / MartialCodexSystem / RiftSystem / GemSystem / ParagonSystem / SeasonSystem` 已有首版脚手架
- 已完成 `P1` 最小闭环接线首版：
  - `B` 键与 `CubePanel` 已接入当前 UI 运行链
  - `SetSystem / MartialCodexSystem / CubeSystem` 已接入 `GameManager / BuildQueryService / SaveManager`
  - `SaveManager v3` 已开始落盘 `martial_codex_state / set_summary / rift / gem / paragon / season`
  - HUD 右侧已补最小传承摘要
  - 当前 `Rift / Gem / Paragon / Season` 仍只有默认状态与存档壳，不含真实玩法

## 4. 已确认的玩法骨架

- 自动横版推进战斗
- 刷装、词条、传奇目标追踪
- 研究树、离线收益、多章节推进
- HUD、背包、研究、图鉴、掉落统计、GM 面板等二级页面

## 5. 当前关键问题判断

- 回流动机不够强
- build 目标感不够前台化
- 阶段事件感偏弱
- 挂机观赏性不足
- 新手理解成本偏高
- 产品包装主要仍停留在文档层

## 6. 当前排期约定

- 已新增 `文档/12_开发排期表.md`
- 当前后续排期已切换为暗黑like 重构顺序：
  1. `Wave 0` 设计冻结
  2. `Wave 1` 状态解耦与查询层
  3. `P1` 传承 + 百炼坊 + 武学秘录
  4. `P2` 试剑秘境 + 宝石 + 华戒
  5. `P3` 宗师修为 + 重入江湖

已完成阶段：

- `P0-1 回流目标层（首版）`
  - 已新增 `DailyGoalSystem`
  - 已有自然日刷新的 `1 主目标 + 2 支线`
  - HUD 已新增独立 `DailyGoalCard`
  - 离线收益弹窗已新增“回来先做什么”
  - 每日目标状态已接入存档
- `P0-2 Build 目标层（首版）`
  - `GameManager` 已新增三流派 build 诊断逻辑
  - HUD 的 `TargetCard` 已升级为 `追踪目标 + 缺口 + 下一件 + 推荐刷图`
  - 每日刷装支线已围绕当前 build 缺口生成
  - 离线收益弹窗已补入 build 缺口与下一件建议
- `P0-3 阶段事件层（首版）`
  - 已新增 `StageEventSystem`
  - Boss 首杀 / 新章节解锁 / 研究关键升级 / 传奇首次获得 / 追踪目标达成已接入统一结果卡
  - `UIOverlayManager` 已支持阶段事件队列
  - 阶段事件状态已接入存档
- `P0-4 观赏性增强层（首版）`
  - HUD 已新增战斗高光条
  - 精英 / Boss 遭遇与击破已接入高光提示
  - 核心技能释放已补入更明确的高光文案
  - 传奇与追踪目标掉落已强化 toast 和掉落视觉参数
- `P1-1 题材包装接入层（文档首版）`
  - 题材方向已从 `灾后黑铁废土` 切换为 `反桃花源修真`
  - 已重写 `10_题材与世界观包装草案.md`
  - 已新增 `13_反桃花源修真命名包装表.md`
  - 已同步更新 `11_首批美术资源清单与外包需求表.md`
- `P1-1 题材包装接入层（高频文案首版）`
  - 章节名已切到 `桃溪外渡 / 祠下灵田`
  - 敌人与 Boss 名已切到 `桃瘴鼠 / 纸甲巡祠 / 伏祠山君 / 祭炉监院` 等新口径
  - 三流派名已切到 `御风道 / 血劫道 / 五雷道`
  - HUD、悟道、异闻录、机缘推演、今日机缘、闭关所得、阶段事件等高频标题已同步接入
  - Godot `--headless --quit-after 1` 已通过，退出码 `0`
- `P1-1 题材包装接入层（中低频文本首版）`
  - `research_tree.json` 已切到悟道/闭关/机缘口径
  - `legendary_affixes.json` 已切到异宝真意/首领专属口径
  - README 已切到 `反桃花源修真` 的对外描述
  - 异闻录详情、闭关结算、掉落前缀、背包分解提示等中低频文本已同步清理
  - 残留核心旧词扫描已通过；Godot 再次 headless 启动退出码 `0`
- `P1-4 Web/展示优化层（原型首版）`
  - 已读取 `Figma_交互设计简介.md` 与 `Figma_分阶段提示词.md`
  - 已重写 `文档/prototype/index.html`
  - 已新增 `文档/prototype/review.html`
  - 新原型已对齐 `1280×720` 主战斗界面结构
  - 已纳入 `机缘追踪 / 今日机缘 / 闭关所得 / 破局要闻 / 5 个主面板` 的首版交互稿
  - 视觉方向已切到 `反桃花源修真` 的网页展示基线
  - 已补出一套更适合评审与汇报的静态展示页
- `P1-4 Web/展示优化层（游戏内样式回灌首版）`
  - 已新增 `scripts/ui/ui_style.gd`
  - HUD、MainNavBar、OfflineReportPopup 已开始接入统一样式层
  - 背包 / 悟道 / 异闻录 / 机缘推演 / GM 面板已接入统一按钮、列表、富文本和进度条样式
  - `BattleSafeFrame` 与右侧 `机缘追踪 / 今日机缘` 已重新回到首页结构中
  - Godot 无界面启动退出码 `0`
  - Godot MCP 调试启动通过，当前仍只剩 `EventBus` signal warning
- `P1-4 Web/展示优化层（布局细化第二轮）`
  - `HUD`、`MainNavBar`、`Inventory`、`Codex`、`DropStats` 场景已继续收紧几何布局
  - 新引入的 `UIStyle` 同名常量 warning 已清掉
  - Godot 再次 headless 启动退出码 `0`
  - Godot MCP 调试启动再次通过，当前仅剩 `EventBus` signal warning
- `P1-4 Web/展示优化层（二级界面结构重排首版）`
  - `InventoryPanel` 已重排为左列表 + 右详情 + 右下操作区
  - `ResearchPanel`、`CodexPanel` 已统一成顶部筛选 + 左列表 + 右详情 + 底部操作
  - `DropStatsPanel` 已改成更偏纵向阅读的推演结构
  - `GMPanel` 已进入统一模态结构
  - `OfflineReportPopup` 已补出收益区与目标建议区底板
  - Godot headless 与 MCP 调试启动均通过，当前仅剩 `EventBus` signal warning

## 7. 文档维护约定

- 从当前轮开始，每次实质性工作完成后都要补一份新的进展记录
- 进展记录路径：
  `文档/项目进展记录/xx_项目进展记录_*.md`
- 如果上下文接近上限，需要优先更新本文件，再继续协作

## 8. 本轮新增内容

- 已根据当前问题分析新增开发排期表
- 已明确将“进展记录持续维护”作为固定协作要求
- 已建立本记忆文件，作为后续上下文切换时的恢复入口
- 已完成 `P0-1 回流目标层` 首版落地
- 已完成 `P0-2 Build 目标层` 首版落地
- 已完成 `P0-3 阶段事件层` 首版落地
- 已完成 `P0-4 观赏性增强层` 首版落地
- 已完成 `P1-1` 的文档首版切换、高频文案接入和中低频文本首版清理
- 已完成本地工程与 GitHub 最新仓库内容重新对齐
- 已完成基于 Figma 文档的网页 UI 原型首版重做
- 已完成静态评审展示页首版导出
- 已完成首轮“网页稿 → 游戏内 UI”样式回灌，并确认项目可运行
- 已完成第二轮布局细化与 warning 收口，项目继续保持可运行
- 已完成所有二级界面的首轮结构重排，并确认项目继续可运行
- 已完成暗黑like 重构 `Wave 0 / Wave 1`：
  - 重写 `Figma_交互设计简介.md`
  - 重写 `Figma_分阶段提示词.md`
  - 重写 `prototype/index.html`
  - 重写 `prototype/review.html`
  - 新增 `BuildQueryService`、`LootRuleService`
  - `EventBus` 已补未来 P1-P3 统一事件
  - `GameManager` 高价值装备自动处理规则已先收紧
  - 新增 `set_defs.json`、`cube_recipes.json`、`gems.json`、`rift_scaling.json`、`rift_keys.json`
  - 新增 `SetSystem / CubeSystem / MartialCodexSystem / RiftSystem / GemSystem / ParagonSystem / SeasonSystem` 脚手架
  - Godot headless 启动继续通过，退出码 `0`
- 已完成 `P1` 最小闭环接线首版：
  - `ui_cube` 输入与 `B` 键已接入
  - 新增 `CubePanel`，包含 `萃取 / 化真 / 重铸 / 互转 / 淬火 / 武学秘录` 六页
  - `set` 装备已开始生成 `set_id / set_name`，并允许携带 1 条武学词条
  - `extract / upgrade_rare / reforge / convert_set / refine_affix` 已通过 `GameManager.execute_cube_recipe()` 进入真实物品流
  - 读档、换装、百炼后会统一重算并广播 `set_bonus_changed / martial_codex_changed`
  - `06_配置表字段说明.md` 已按真实 schema 重写
- 已完成 `P2` 最小切片首版：
  - `RiftSystem` 已改为真实运行态，包含 `owned_keys / recent_results / active_run`
  - 试剑秘境已开始复用当前战斗节点作为基底，并把 `HP / 伤害 / 奖励倍率 / 时限` 接进真实战斗
  - 首领节点通关后会掉落 `试剑令`
  - 秘境通关已可发放 `宝石` 与 `华戒` 奖励
  - `GemSystem` 已支持拥有、自动首镶、双饰品位切换和 combat bonuses
  - `华戒` 已接入 `SetSystem` 的 `+2 件数` 计算
  - 现有 `P` 面板已临时升级为 `推演与秘境`，可直接开秘境、看钥匙、切换宝石
  - Godot MCP 调试启动再次通过，当前仍只剩既有 `EventBus` signal warning
- 已完成 `U / P` 正式化重构首版：
  - `U` 入口已正式升级为 `成长中心`，保留原 `research` 面板 id 与 `ui_research` 快捷键
  - `成长中心` 已拆成 `武学参悟 / 宗师修为 / 重入江湖` 三页
  - `ParagonSystem.build_runtime_summary()` 与 `SeasonSystem.build_runtime_summary()` 已提供占位摘要接口
  - `P` 入口已正式升级为多页 `推演与秘境`，保留原 `drop_stats` 面板 id 与 `ui_drop_stats` 快捷键
  - `推演与秘境` 已拆成 `机缘推演 / 试剑秘境 / 秘境结算` 三页
  - `RiftSystem.build_runtime_summary()` 已补 `recommended_level / can_start / blocked_reason`
  - `GameManager` 已新增 `get_progression_hub_summary()` 与 `get_analysis_hub_summary()`
  - 秘境结算摘要已持久化回 `rift_state.recent_results`
  - `MainNavBar`、面板标题与提示文案已统一到 `成长中心 / 推演与秘境` 术语
  - Godot MCP 调试启动再次通过，当前仍只剩既有 `EventBus` signal warning
- 已完成 `P3` 最小可玩闭环首版：
  - `ParagonSystem` 已从占位态扩展为真实修为状态机，包含 `is_unlocked / level / experience / available_points / allocated`
  - 当前版本将“满级后修为”落地为“击破第二章首领后，推图与秘境收益开始转化为宗师修为”
  - `成长中心 -> 宗师修为` 页已支持真实点数分配，当前包含攻击、生命、防御、暴击、暴伤、香火钱、修为获取七项
  - `BuildQueryService` 已开始叠加 `Paragon / Season` 的 combat bonuses
  - `MetaProgressionSystem` 已开始叠加 `Paragon / Season` 的 meta bonuses
  - `SeasonSystem` 已从占位态扩展为真实轮回状态机，当前条件为“击破第二章首领 + 试剑秘境最高层达到 20”
  - `成长中心 -> 重入江湖` 页已支持二次确认式轮回
  - 重入江湖当前会保留 `武学秘录 / 宗师修为 / 江湖见闻录 / 江湖阅历`，重置装备、背包、材料、章节、秘境、宝石与武学参悟
  - 江湖阅历永久加成已开始影响伤害、香火钱、修为获取与掉落品质
  - 装备稀有度投点已开始吃 `drop_quality_bonus` 的一档提升
  - Godot MCP 调试启动再次通过，当前仍只剩既有 `EventBus` signal warning
- 已完成 `P3` 体验补强第二轮：
  - `成长中心 -> 宗师修为` 已新增 `重置分点` 按钮，当前为免费返还全部已投点数
  - `SeasonSystem.build_rebirth_preview()` 已补 `current_bonus_text / next_bonus_text`
  - `成长中心 -> 重入江湖` 预览已改为明确展示“当前永久加成 / 下次永久加成”
- 已完成 `RPG 化系统与交互重构` 第一切片：
  - 新增 `ItemPresentationService / InventoryViewModelService / EquipmentViewModelService / CubeViewModelService`
  - `GameManager` 已新增 `get_inventory_screen_state / get_equipment_screen_state / get_cube_screen_state / unequip_slot`
  - `InventoryPanel` 已切换到纸娃娃 + 格子库存结构，不改底层存档数组
  - `CubePanel` 已切换到工坊式三栏结构，继续复用现有百炼真实运行链
  - HUD 标题口径已收口为 `当前 Build / 当前目标 / 已穿装备 · 9槽`
  - Godot MCP 启动验证通过，无新增 parse/runtime error，当前仍只剩既有 warning
- 已完成 `RPG 化系统与交互重构` 第二切片：
  - `GameManager` 已新增一次性 `UI Focus Request` 机制，用于 HUD -> 面板 的聚焦跳转
  - `InventoryPanel` 打开时已支持消费焦点请求，可直接聚焦某件物品或某个已穿槽位
  - `CubePanel` 打开时已支持消费焦点请求，可直接进入指定配方页并锁定候选目标
  - 掉落高光数据已补 `item_id / target_slot`
  - 点击 `最近掉落` 时，当前可直接分流到 `背包` 或 `百炼坊 -> 萃取武学`
  - 点击 `当前 Build / 当前目标 / 已穿装备` 也已可直接打开对应主面板
  - Godot MCP 启动验证再次通过，无新增 parse/runtime error
- 已完成 `RPG 化系统与交互重构` 第三切片：
  - 新增统一卡片组件 `ItemCardButton`
  - `InventoryPanel` 的纸娃娃槽位已切到统一卡片组件
  - `InventoryPanel` 的 10x4 格子库存已切到统一卡片组件
  - `CubePanel` 的候选物列表已切到统一卡片组件
  - 统一卡片组件当前已统一标题、副标题、标签、选中态、占位态、tooltip 与品质边框色
  - Godot MCP 启动验证再次通过，无新增 parse/runtime error
  - 本轮已继续把 `ItemCardButton` 升级为更接近正式 RPG 物品格的形态
  - 当前统一卡片组件已补图标区、品质框贴图与角标
  - `InventoryPanel` 纸娃娃、格子背包与 `CubePanel` 候选物已开始统一向卡片传递 `slot_id / rarity`
  - 卡片角标当前已支持 `传承 / 武学 / 高价值 / 已精炼 / 已锁定` 优先级展示
  - Godot MCP 启动验证再次通过，无新增 parse/runtime error
  - `重入江湖` 完成后会直接触发一张 `stage_event` 回流提示卡，告诉玩家当前已保留内容、永久加成和下一步建议
  - Godot MCP 调试启动再次通过，当前仍只剩既有 `EventBus` signal warning
- 已完成第三章内容切片首版：
  - `chapter_2.next_chapter_id` 已接到 `chapter_3`
  - 新增第三章 `铁锁镇`
  - 已补 `ch3_n1 ~ ch3_boss` 五个节点
  - 已补第三章敌人池、掉落档位与 Boss `银钩老人`
  - 第三章背景色、节点背景与 Boss 立绘映射已接入现有战斗表现链
  - 当前第三章视觉资源仍以复用占位为主，但 Godot MCP 调试启动已通过，未新增 parse/runtime error
- 已完成节点命名与显示语义正式化：
  - 当前三章主线节点已补正式 `name`
  - `ConfigDB` 已新增统一节点显示 helper
  - HUD、推演、GM、掉落推荐、掉落统计和战斗高光已改为显示正式节点名，而不是直接暴露 `ch1_n1 / ch3_n2` 这类内部 ID
  - Godot MCP 调试启动再次通过，当前仍只剩既有 `EventBus` signal warning
- 已完成美术资源全量替换首版：
  - 当前项目正式视觉方向已冻结为“水墨写意 + 暗黑like 武侠材质”
  - 已新增 `tools/generate_art_assets.py`，可批量生成当前工程资源与文档后备资源
  - 已生成 `文档/美术资源执行清单_v1.md`、`文档/美术提示词清单_v1.md`、`文档/美术资源生成摘要_v1.md`
  - 当前清单资源总数为 `129`，其中当前工程已接线资源 `43`，未接线入库资源 `86`
  - 第一章、第二章背景，第三章 `铁锁镇` 双背景，三位 Boss，主角三态、通用敌人头像、主要系统图标、掉落图标、品质框和结果卡底板都已落到正式资源
  - 第三章已不再复用旧背景和第二章 Boss 图；`CombatSystem`、`EnemyActor`、`OfflineReportPopupController` 已切到 `bg_tiesuo_zhen_v1a / bg_tiesuo_zhen_v1b / boss_yingou_laoren_v1`
  - 本轮生成方式为本地程序化生成，目标是风格统一、工程可运行与批量可维护，不等同于逐张原画精修
  - Godot MCP 调试启动再次通过，无 `missing resource`，当前仍只剩既有 `EventBus` signal warning
- 已完成当前运行资源的 OpenAI GPT 生图替换：
  - 已新增 `tools/generate_openai_art_assets.py`，可基于现有清单直接调用 OpenAI Images API
  - 脚本支持 `wired / unwired / all` 范围筛选、透明背景、自动中心裁切、自动缩放和尺寸校验
  - 已先用 `bg_tiesuo_zhen_v1a` 做最小验证，再将当前已接线的 `43` 个运行资源全部替换为 GPT 生图版本
  - 当前运行中的背景、Boss、主角、通用敌人头像、主要图标、品质框和结果卡底板，都已经不再是程序化占位，而是 OpenAI 实际出图
  - 已新增原始大图目录 `assets/generated/_openai_raw/`
  - 已新增生成日志 `文档/美术资源_openai生成日志_v1.jsonl`
  - Godot MCP 调试启动再次通过，无 `missing resource`、无新增 parse/runtime error；当前剩余 warning 主要是既有 `EventBus` signal warning 与既有常量名 warning
- 已完成主角动画状态机首版：
  - 主角视觉节点已从静态 `Sprite2D` 升级为 `AnimatedSprite2D`
  - `PlayerActor` 已接入 `idle / move / combat / attack` 四态切换
  - 普攻与核心技能都会触发一次性 `attack` 动画，结束后自动回到当前应有状态
  - 已新增 `hero_move_anim_01 ~ 04` 与 `hero_attack_anim_01 ~ 04`
  - 这 `8` 张动作帧都已由 OpenAI 实际生成并替换为正式资源
  - `RuntimeTextureLoader` 已补“无 `.import` 时直接读磁盘 PNG”的兜底逻辑
  - 当前资源总数已更新为 `137`，其中已接线资源 `51`，未接线入库资源 `86`
  - Godot MCP 调试启动再次通过，无 `missing resource`、无新增 parse/runtime error

## 9. 下次接手时优先做什么

如果是继续推进开发，而不是单纯查阅信息，建议优先从以下动作开始：

1. 阅读 `文档/项目进展记录/58_项目进展记录_v4.8.md`
2. 阅读 `文档/项目进展记录/59_项目进展记录_v4.9.md`
3. 阅读 `文档/项目进展记录/60_项目进展记录_v5.0.md`
4. 在 Godot 内手动回归主角 `move / attack / combat` 状态切换体感
5. 决定是先把主角动作扩到 `6-8` 帧，还是继续给普通敌人补第一套动画
