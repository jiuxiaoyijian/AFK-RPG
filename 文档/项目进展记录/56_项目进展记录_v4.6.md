# 项目进展记录 v4.6

日期：2026-03-25

---

## 本轮目标

在 `P3` 体验补强已经完成的基础上，继续把主线内容往前推一段，先补出一个可运行的第三章切片，而不是直接跳到新系统扩容。

本轮聚焦两件事：

1. 接入第三章 `铁锁镇`
2. 保持当前运行链可启动、可读配置、可衔接现有章节推进

---

## 已完成

### 1. 主线已从两章扩展到三章

本轮在 `chapter_defs.json` 中完成了第三章接入：

- `chapter_2.next_chapter_id` 已指向 `chapter_3`
- 新增 `chapter_3`
- 章节名定为 `铁锁镇`

当前第三章结构：

- `ch3_n1`
- `ch3_n2`
- `ch3_n3`
- `ch3_n4`
- `ch3_boss`

节点类型保持当前熟悉节奏：

- 前两节点为普通战
- 中间两节点为精英战
- 最后一节点为 Boss 战

这意味着现有“清章 -> 解锁下一章”的主线推进链路，现在已经可以自然继续走到第三章。

### 2. 第三章敌人与掉落已补齐

为了让第三章不是空壳，本轮同步补了敌人与掉落配置：

新增敌人：

- `铁索打手`
- `赌坊刀客`
- `码头钩卒`
- `锁街巡头`
- `黑巷钩卫`
- `银钩老人`

新增掉落档位：

- `drop_normal_ch3`
- `drop_elite_ch3`
- `drop_boss_ch3`

当前第三章的奖励曲线相对第二章继续上抬，包含更高的：

- 香火钱
- 废料
- 核心材料
- 首领碎片
- 装备掉落强度

这样可以让第三章在当前重构节奏里承担“第二章毕业后的新刷图层”，而不是只作为一个地图名占位。

### 3. 第三章战斗表现已接入现有视觉链

本轮同步补了第三章的章节色板、节点背景和 Boss 立绘映射，因此不会出现“章节能进，但战斗表现缺引用”的断链问题。

当前实现策略是：

- 先复用现有背景图资源
- 先复用现有 Boss 立绘资源
- 通过 `chapter_3 / ch3_* / boss_silver_hook_elder` 的映射接进现有表现层

这保证了本轮目标优先是“主线可继续跑”，而不是卡在新资源生产上。

---

## 验证结果

本轮已再次通过 Godot MCP 调试启动验证。

结果：

- 无新增 parse error
- 无新增 runtime error
- 当前仍只有既有 `EventBus` signal warning

说明第三章新增的：

- 章节配置
- 敌人配置
- 掉落配置
- 背景映射
- Boss 立绘映射

都已经至少通过了启动层验证，没有把项目直接打断。

---

## 当前边界

本轮是“第三章内容切片接入”，不是“第三章完整美术与剧情封装”，所以边界仍然很明确：

- 第三章当前主要是玩法与配置层接入，不含独立剧情事件链
- 第三章视觉资源目前大量复用已有背景与 Boss 图，占位性质明确
- 还没有完整手动走完“第二章 Boss -> 第三章解锁 -> 第三章首领”的整条游玩回归
- 第三章只是把主线继续往前推了一章，不代表第四章或新的长期系统已经开始

---

## 下一步建议

下一轮建议优先顺序：

1. 在编辑器里手动验证 `第二章 Boss -> 第三章解锁 -> 第三章刷图 -> 第三章 Boss`
2. 给第三章补更匹配 `铁锁镇` 题材的专属背景与 Boss 视觉资源
3. 根据第三章体验结果，决定是继续补第四章，还是先回头做第三章的阶段事件与题材包装
4. 若主线节奏已经稳定，再考虑推进下一段长期内容扩容
