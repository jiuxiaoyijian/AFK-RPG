# 【配置表名称】字段说明

> 作者：  
> 创建日期：YYYY-MM-DD  
> 最后更新：YYYY-MM-DD  
> 对应数据文件：`data/xxx/xxx.json`

---

## 1. 概述

简要描述本配置表的用途、被哪些系统读取、影响哪些游戏行为。

## 2. 字段定义

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | String | 是 | — | 唯一标识符 |
| `name` | String | 是 | — | 显示名称 |
| `value` | Float | 是 | 0.0 | 数值描述 |
| `tags` | Array[String] | 否 | [] | 标签列表 |

## 3. 枚举值说明

### 3.1 type 枚举

| 值 | 含义 |
|----|------|
| `"melee"` | 近战 |
| `"ranged"` | 远程 |

## 4. 配置示例

```json
[
  {
    "id": "example_001",
    "name": "示例条目",
    "value": 10.0,
    "tags": ["tag_a"]
  }
]
```

## 5. 数值设计说明

描述数值的设计意图、成长曲线、平衡考量等。

## 6. 验证规则

- [ ] `id` 不可重复
- [ ] `value` 必须 > 0
- [ ] 引用的外部 ID 必须在对应表中存在

## 7. 测试用例

### 用例 1：正常加载

- **输入**：上方配置示例
- **预期**：系统正确读取并应用数值

### 用例 2：异常数据

- **输入**：缺少必填字段的数据
- **预期**：系统输出警告日志，使用默认值

## 8. 变更记录

| 日期 | 变更内容 | 负责人 |
|------|----------|--------|
| YYYY-MM-DD | 初稿 | xxx |
